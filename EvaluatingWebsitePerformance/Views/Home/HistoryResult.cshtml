@model EvaluatingWebsitePerformance.Models.BaseRequestViewModel
@{
    ViewBag.Title = "History result";
}

@if (Model == null)
{
    <br />
    <h3>Website not found</h3>

    <div>
        &rsaquo; @Html.ActionLink("Back to main page", "Index", "Home")
    </div>
}
else
{
    @Html.Partial("Results", Model)
}